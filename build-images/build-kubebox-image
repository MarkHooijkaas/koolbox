#!/usr/bin/env bash
set -eu

script_dir=$(dirname ${BASH_SOURCE[0]})

export KOOLBOX_BUILD_FROM=debian:trixie
export KOOLBOX_IMAGE_NAME=kubebox
export KOOLBOX_BUILD_TAGS="full"

source $script_dir/inc/kubebox-apt.inc
source $script_dir/inc/interactive-apt.inc

$script_dir/koolbox-image-builder "${@}"
